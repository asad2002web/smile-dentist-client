
const Title = ({miniTitle, Title}) => {
  return (
    <div className="text-center mb-12">
        <h6 className="uppercase MiniTitle pb-3">{miniTitle}</h6>
        <h2 className="Heading2">{Title}</h2>
    </div>
  )
}

export default Title;

